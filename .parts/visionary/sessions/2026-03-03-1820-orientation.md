# Visionary session — 2026-03-03 6:20pm orientation

## Boot context

Re-orientation session. State file existed from cold boot earlier today. No prior session files.

Read: SKILL.md, PARTS.md, cold boot orientation, working file (Product + Values + CTO already written), state file, stack state, visionary config, coordination-store.ts, renderer.ts (full), main.cjs.

## What happened

Wrote updated Visionary h2 section to orientation working file (`orientation-2026-03-03-1820.md`). Key updates from cold boot:

1. **Standalone Electron dispatch console shipped** — read the full 1,336-line renderer. Three-zone layout, attention queue with priority sorting, keyboard shortcuts, orchestration parsing ported from plugin. This is a material change to the project's shape.

2. **Coordination store added** — cross-surface session/handoff model at `~/.augment/workspaces/`. Read the full 250-line implementation. Architecture is clean (JSON files, SHA-1 workspace IDs, write→consume handoff protocol). Not wired to UI.

3. **Hetzner VPS replaces Mac Mini** — from stack state. Changed my "Mac Mini bridge" opportunity to "VPS as headless dispatch target." Same thesis, different hardware.

## Key observations

- Identified "attention-driven dispatch" as the core product concept, distinct from terminal-app framing. The attention queue (`Cmd+J` cycle, priority-sorted sidebar, attention badges clearing on focus) is the interaction pattern that differentiates Augment from all competitors. No tmux wrapper or GUI skin does this.

- CC hooks (PostToolUse, etc.) identified as the highest-leverage technical investigation for replacing regex parsing fragility. Proposed validation: hook writes JSON to file, renderer watches via `fs.watch`.

- Cross-surface handoff is architecturally unique. No competitor spans host environments for the same workspace.

## State changes

Updated `.state/current.md` with:
- Revised technology bets (added attention-driven dispatch, cross-surface management)
- Updated opportunities (VPS headless target replaces Mac Mini bridge, CC hooks opportunity added)
- Updated trajectory (attention dispatch as novel interaction pattern, cross-surface handoff as unique capability)
- Updated risks (two-surface maintenance burden added)

## In-progress

Nothing — orientation section written, state updated, session log complete.
