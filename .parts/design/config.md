# Design config — Augment

## Scan targets

- `src/terminal-view.ts` — primary UI surface
- `src/terminal-manager-view.ts` — management/list view
- `src/terminal-switcher.ts` — switching interaction
- `styles.css` — current visual patterns

## Design context

- Standalone Electron app is the current focus surface
- Keyboard-first: Matt works in terminals
