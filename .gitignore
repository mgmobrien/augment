node_modules
.DS_Store
electron/
dist-electron/

# AI team working files — ephemeral, regenerated each session
.parts/

# Build artifacts and dev test files
build/
spinner-test.html

# Windows updater — saved vault path (user-local, not committed)
scripts/update.cfg

# Per-developer vault path for deploy.sh (set once: echo "/path/to/vault" > .vault-path)
.vault-path

# Visual test environment — generated by scripts/setup-test.sh, not committed
test-vault/
test-config/
playwright-report/
test-results/
